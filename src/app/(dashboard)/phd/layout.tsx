/* eslint-disable react-hooks/exhaustive-deps */
'use client';

export default function PhDLayout({ children }: { children: React.ReactNode }) {
  // Authorization is handled by individual pages/API routes
  return <>{children}</>;
}
